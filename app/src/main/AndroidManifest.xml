<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hanieum">
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".User_Create"></activity>
        <activity android:name=".Cloud" />
        <activity android:name=".Message" />
        <activity android:name=".HospitalLocation" />
        <activity android:name=".PsychoTherapy" />
        <activity android:name=".ReservationCheck" />
        <activity android:name=".HospitalReservation" />
        <activity android:name=".HealthCare" />
        <activity android:name=".VideoCallDetails" />
        <activity android:name=".VoiceCallDetails" />
        <activity android:name=".VoiceCall" />
        <activity android:name=".VideoCall" />
        <activity android:name=".Communication" />
        <activity android:name=".MainActivity" />
        <activity android:name=".User_Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>